int l = 0, r = ans.size() - 1;
  while (l <= r) {
    if (l == r) {
      ans1 = ans[l];
      break;
    }
    int mid = (l + r) / 2;
    vector<int> lft;
    for (int i = l; i <= mid; i++) {
      lft.push_back(ans[i]);
    }
    bool res = query(lft);
    if (res)
      r = mid;
    else
      l = mid + 1;
  }